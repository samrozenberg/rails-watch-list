<div class="container">
  <h2><%= @list.name %></h2>

  <% @list.movies.each do |movie| %>
  <p><%= movie.title %> | Rating: <%= movie.rating %> | Comment:  <%= movie.bookmarks[0].comment %></p>
  <img src="<%= movie.poster_url %>" alt="" width="150px">
  <%= link_to "🗑", destroy_path(movie.bookmarks[0].id), method: :delete %>
  <br>
  <%= movie.overview %>
  <br>
  ---------------------------------------------------
  <p><% end %></p>

  <%= link_to "Add a New Bookmark", new_list_bookmark_path(@list), class: "btn btn-primary" %>
  <%= link_to "Back to All Lists", lists_path %>
</div>
